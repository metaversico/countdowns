FROM docker.io/denoland/deno:alpine
WORKDIR /app
COPY backend/ .
RUN deno cache main.ts
EXPOSE 8000
# Create data directory for Deno KV
RUN mkdir -p /app/data
CMD ["deno", "run", "--allow-net", "--allow-env", "--unstable-kv", "main.ts"]
